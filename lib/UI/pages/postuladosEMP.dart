import 'package:flutter/material.dart';

class postuladosemp extends StatelessWidget {
  const postuladosemp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: const Text(' POSTULADOS'),
        backgroundColor: Colors.black,
      ),
    );
  }
}
